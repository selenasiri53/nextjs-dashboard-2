import SideNav from '@/app/ui/dashboard/sidenav';
 
export default function Layout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex h-screen flex-col md:flex-row md:overflow-hidden">
      <div className="w-full flex-none md:w-64">
        <SideNav />
      </div>
      <div className="flex-grow p-6 md:overflow-y-auto md:p-12">{children}</div>
    </div>
  );
}

// Partial rendering - the layout won't rerender, only the component will load
// In Nextjs the layout has children props - in this case its the dashboard page, customers' and invoices' pages
// that will be nested inside

// contains the root layout